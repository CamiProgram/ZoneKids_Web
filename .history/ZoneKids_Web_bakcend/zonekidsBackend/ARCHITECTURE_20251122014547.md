# 🏗️ Arquitectura de ZoneKids Backend - Evaluación 3

## Diagrama General de la Arquitectura

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Frontend/Mobile)                         │
└────────────────────────────────────┬──────────────────────────────────────┘
                                     │
                    HTTP/HTTPS REST API (/api/v1/...)
                                     │
┌────────────────────────────────────▼──────────────────────────────────────┐
│                    API GATEWAY / LOAD BALANCER                            │
└────────────────────────────────────┬──────────────────────────────────────┘
                                     │
┌────────────────────────────────────▼──────────────────────────────────────┐
│                      SPRING BOOT APPLICATION (8080)                       │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                   Security & Interceptors                         │  │
│  │  ┌──────────────────────┐        ┌──────────────────────────────┐│  │
│  │  │ JwtAuthenticationFilter│──────│ JwtUtils                     ││  │
│  │  │ (Valida Token)        │        │ (Genera/Valida JWT)         ││  │
│  │  └──────────────────────┘        └──────────────────────────────┘│  │
│  │  ┌──────────────────────┐        ┌──────────────────────────────┐│  │
│  │  │ SecurityConfig        │──────│ CustomUserDetailsService    ││  │
│  │  │ (RBAC Rules)          │        │ (Carga usuarios de BD)       ││  │
│  │  └──────────────────────┘        └──────────────────────────────┘│  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                     Controller Layer                              │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐       │  │
│  │  │ AuthController       │ ProductoController       │ UsuarioController│  │
│  │  │ /api/v1/auth │ /api/v1/productos     │ /api/v1/usuarios │       │  │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘       │  │
│  │     │                  │                  │                      │  │
│  │     │ Login/Register   │ CRUD Productos   │ CRUD Usuarios         │  │
│  │     │ Tokens           │ + Imágenes       │ (ADMIN only)         │  │
│  │  ┌──────────────────────────────────────────────────────────────┐│  │
│  │  │ OrdenController                                              ││  │
│  │  │ /api/v1/ordenes                                              ││  │
│  │  │ (Crear compras, Gestionar órdenes)                          ││  │
│  │  └──────────────────────────────────────────────────────────────┘│  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                      Service Layer                                │  │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐ │  │
│  │  │ UserService     │  │ ProductoServices│  │ OrdenService     │ │  │
│  │  │ (Lógica usuarios)│ │ (Lógica productos)  │ (Lógica órdenes) │ │  │
│  │  └─────────────────┘  └─────────────────┘  └──────────────────┘ │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                    Repository Layer                              │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐       │  │
│  │  │ UserRepository   │ ProductoRepository  │ OrdenRepository      │  │
│  │  │ (JPA)        │  │ (JPA)        │  │ (JPA)           │       │  │
│  │  └──────────────┘  └──────────────┘  └──────────────────┘       │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                     │                                     │
└─────────────────────────────────────┼─────────────────────────────────────┘
                                      │
┌─────────────────────────────────────▼─────────────────────────────────────┐
│                          MySQL Database                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────┐  │
│  │ usuarios     │  │ productos    │  │ ordenes                      │  │
│  │ - id (PK)    │  │ - id (PK)    │  │ - id (PK)                    │  │
│  │ - nombre     │  │ - nombre     │  │ - usuario_id (FK)            │  │
│  │ - email      │  │ - precio     │  │ - total                      │  │
│  │ - contrasena │  │ - stock      │  │ - estado                     │  │
│  │ - rol        │  │ - categoria  │  │ - fecha                      │  │
│  │ - estado     │  │ - estado     │  │                              │  │
│  │              │  │              │  │ detalle_ordenes              │  │
│  │              │  │              │  │ - id (PK)                    │  │
│  │              │  │ producto_imagenes  │ - orden_id (FK)          │  │
│  │              │  │ - producto_id (FK) │ - producto_id (FK)       │  │
│  │              │  │ - imagen_url       │ - cantidad                 │  │
│  │              │  │                    │ - precioUnitario           │  │
│  └──────────────┘  └──────────────┘  └──────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Flujo de Autenticación

```
┌─────────────┐
│   Cliente   │
└──────┬──────┘
       │
       │ 1. POST /api/v1/auth/login
       │    {"email": "...", "contrasena": "..."}
       │
       ▼
┌─────────────────────────────────┐
│  AuthController.login()          │
│  ├─ Busca usuario por email      │
│  ├─ Valida estado (activo)       │
│  └─ Autentica con credenciales   │
└──────┬──────────────────────────┘
       │
       │ 2. AuthenticationManager.authenticate()
       │    (Valida contra BD + BCrypt)
       │
       ▼
┌─────────────────────────────────┐
│  JwtUtils.generateToken()        │
│  ├─ Crea JWT (email como sub)    │
│  ├─ Válido por 24 horas          │
│  └─ Firma con HS256 + secret     │
└──────┬──────────────────────────┘
       │
       │ 3. Response: {"token": "...", "mensaje": "..."}
       │
       ▼
┌─────────────┐
│   Cliente   │ (Guarda token en localStorage/sessionStorage)
└─────────────┘
       │
       │ 4. Próximas requests incluyen token
       │    Authorization: Bearer eyJh...
       │
       ▼
┌──────────────────────────────────────┐
│  JwtAuthenticationFilter             │
│  ├─ Lee header Authorization         │
│  ├─ Extrae token (quita "Bearer ")   │
│  └─ Valida con JwtUtils              │
└──────┬───────────────────────────────┘
       │
       │ 5a. Token válido
       │     └─► Carga usuario con CustomUserDetailsService
       │        └─► Autentica en SecurityContextHolder
       │           └─► Request continúa
       │
       │ 5b. Token inválido/expirado
       │     └─► Rechaza request (401/403)
       │
       ▼
┌──────────────────────────┐
│  Endpoint protegido      │
│  (ProductoController,    │
│   UsuarioController, etc)│
└──────────────────────────┘
```

---

## 📦 Relaciones entre Entidades

```
User (1)
  │
  ├─────────────────────────────── Rol (Enum)
  │                                ADMIN
  │                                VENDEDOR
  │                                CLIENTE
  │
  └─────(1:N)─────────────────── Orden (*)
                                   │
                                   ├─── Estado: pendiente/completada/cancelada
                                   │
                                   └─────(1:N)─────────────────── DetalleOrden (*)
                                                                   │
                                                                   ├─── Cantidad
                                                                   │
                                                                   └─(N:1)─ Producto
                                                                            │
                                                                            ├─── Nombre
                                                                            ├─── Precio
                                                                            ├─── Stock
                                                                            │
                                                                            └─(1:N)─ ProductoImagenes (2-3)
                                                                                     │
                                                                                     └─── URL
```

---

## 🔄 Flujo de Creación de Orden

```
┌───────────────────────────────────────────────────────────────────┐
│ CLIENTE REALIZA COMPRA                                            │
└───────────────────┬─────────────────────────────────────────────┘
                    │
        1. POST /api/v1/ordenes
           {"usuarioId": 1, "detalles": [...]}
                    │
        ┌───────────▼───────────┐
        │ OrdenController.crear()│
        └───────────┬───────────┘
                    │
        2. Validar usuario existe
           └── ✅ Usuario encontrado
                    │
        3. Procesar cada detalle:
           Para cada DetalleOrdenRequestDto:
           │
           ├─ Buscar producto
           ├─ Validar stock >= cantidad
           ├─ Crear DetalleOrden
           ├─ Actualizar stock producto
           │  (stock = stock - cantidad)
           │
           └─ Agregar a orden
                    │
        4. OrdenService.crear()
           └── Calcular total
               (suma de subtotales)
                    │
        5. Guardar en BD
           ├─ INSERT en ordenes
           └─ INSERT en detalle_ordenes
                    │
        6. Response 201 Created
           {"id": 1, "total": 99.98, "estado": "pendiente", ...}
                    │
        ┌───────────▼───────────┐
        │ ORDEN CREADA          │
        │ STOCK ACTUALIZADO     │
        └───────────────────────┘
```

---

## 🛡️ Control de Acceso por Rol

```
┌─────────────────────────────────────────────────────────────────────┐
│ ENDPOINT: GET /api/v1/productos                                    │
│ @PreAuthorize("hasAnyRole('ADMIN', 'VENDEDOR', 'CLIENTE')")       │
└──────────────────┬────────────────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
    Token ADMIN      Token VENDEDOR      Token CLIENTE      Sin Token
        │                     │                    │              │
        ▼                     ▼                    ▼              ▼
    ✅ Acceso          ✅ Acceso         ✅ Acceso       ❌ 401 Unauthorized
                                                               (Requiere token)


┌─────────────────────────────────────────────────────────────────────┐
│ ENDPOINT: POST /api/v1/productos                                   │
│ @PreAuthorize("hasRole('ADMIN')")                                  │
└──────────────────┬────────────────────────────────────────────────┘
                   │
        ┌──────────┼──────────┬──────────┐
        │          │          │          │
    Token ADMIN   Token VEN   Token CLI  Sin Token
        │          │          │          │
        ▼          ▼          ▼          ▼
    ✅ Acceso   ❌ 403       ❌ 403    ❌ 401
                Forbidden    Forbidden Unauthorized


┌─────────────────────────────────────────────────────────────────────┐
│ ENDPOINT: PATCH /api/v1/usuarios/{id}/estado                       │
│ @PreAuthorize("hasRole('ADMIN')")                                  │
└──────────────────┬────────────────────────────────────────────────┘
                   │
    Solo ADMIN tiene acceso
        │
        ▼
    ✅ ADMIN: Puede cambiar estado de cualquier usuario
    ❌ VENDEDOR: Access Denied (403)
    ❌ CLIENTE: Access Denied (403)
```

---

## 🚀 Flujo Completo: Cliente Comprando

```
START
  │
  ├─ 1. Cliente se registra
  │   └─ POST /api/v1/auth/register
  │      └─ Rol asignado automáticamente: CLIENTE
  │
  ├─ 2. Cliente hace login
  │   └─ POST /api/v1/auth/login
  │      └─ Recibe JWT Token
  │
  ├─ 3. Cliente ve productos
  │   └─ GET /api/v1/productos
  │      └─ Con Header: Authorization: Bearer <token>
  │      └─ Respuesta: Lista de productos con 2-3 imágenes
  │
  ├─ 4. Cliente selecciona productos y crea orden
  │   └─ POST /api/v1/ordenes
  │      └─ Payload: {usuarioId, detalles: [{productoId, cantidad}]}
  │      └─ Sistema:
  │         ├─ Valida usuario
  │         ├─ Valida stock
  │         ├─ Crea orden con estado "pendiente"
  │         └─ Actualiza stock
  │
  ├─ 5. Cliente ve sus órdenes
  │   └─ GET /api/v1/ordenes
  │      └─ Respuesta: Lista de órdenes del cliente
  │
  └─ 6. Admin gestiona la orden
     └─ PATCH /api/v1/ordenes/{id}/estado?estado=completada
        └─ Cambia estado a "completada"

END
```

---

## 📊 Tabla de Permisos Detallada

| Endpoint | Método | Rol Requerido | Descripción |
|----------|--------|---|---|
| `/api/v1/auth/login` | POST | PÚBLICO | Autenticar usuario |
| `/api/v1/auth/register` | POST | PÚBLICO | Registrar nuevo usuario |
| `/api/v1/productos` | GET | ADMIN/VENDEDOR/CLIENTE | Listar productos |
| `/api/v1/productos/{id}` | GET | ADMIN/VENDEDOR/CLIENTE | Ver detalle producto |
| `/api/v1/productos` | POST | ADMIN | Crear producto |
| `/api/v1/productos/{id}` | PUT | ADMIN | Actualizar producto |
| `/api/v1/productos/{id}` | DELETE | ADMIN | Eliminar producto |
| `/api/v1/usuarios` | GET | ADMIN | Listar usuarios |
| `/api/v1/usuarios/{id}` | GET | ADMIN | Ver detalle usuario |
| `/api/v1/usuarios` | POST | ADMIN | Crear usuario |
| `/api/v1/usuarios/{id}` | PUT | ADMIN | Actualizar usuario |
| `/api/v1/usuarios/{id}/estado` | PATCH | ADMIN | Cambiar estado usuario |
| `/api/v1/usuarios/{id}` | DELETE | ADMIN | Eliminar usuario |
| `/api/v1/ordenes` | GET | ADMIN/VENDEDOR | Listar órdenes |
| `/api/v1/ordenes/{id}` | GET | ADMIN/VENDEDOR | Ver detalle orden |
| `/api/v1/ordenes` | POST | ADMIN/CLIENTE | Crear orden |
| `/api/v1/ordenes/{id}/estado` | PATCH | ADMIN | Cambiar estado orden |
| `/api/v1/ordenes/{id}` | DELETE | ADMIN | Eliminar orden |

---

## 🔍 Validaciones en Cascada

```
Request → DTO Validation (@Valid)
           ├─ @NotBlank
           ├─ @Email
           ├─ @Min/@Max
           └─ Custom validators
                    │
                    ▼
        Entity Validation (@PrePersist/@PreUpdate)
        ├─ Validar imágenes (2-3)
        ├─ Validar precios >= 0
        └─ Validar stocks >= 0
                    │
                    ▼
        Business Logic Validation (Services)
        ├─ Verificar usuario existe
        ├─ Verificar email único
        ├─ Verificar stock suficiente
        └─ Verificar roles
                    │
                    ▼
        Database Constraints (MySQL)
        ├─ PRIMARY KEY
        ├─ UNIQUE (email)
        ├─ FOREIGN KEY
        └─ CHECK constraints
                    │
                    ▼
        ✅ GUARDADO EN BD o ❌ ERROR
```

---

## 📈 Escalabilidad

**Estado Actual:** Single-instance
**Para Producción:**
- Load Balancer (nginx/HAProxy)
- Multiple instancias de Spring Boot
- Redis para caché de sesiones
- Elasticsearch para logs

```
┌──────────┐
│ Cliente  │
└────┬─────┘
     │
     ▼
┌─────────────────────────────────┐
│  Load Balancer (nginx)          │
│  ├─ /api/v1/* → Backend 1:8080  │
│  ├─ /api/v1/* → Backend 2:8080  │
│  └─ /api/v1/* → Backend 3:8080  │
└──────────────────────────────────┘
     │
     ├─────────────┬─────────────┐
     ▼             ▼             ▼
  [Boot:1]     [Boot:2]     [Boot:3]
  (8080)       (8080)       (8080)
     │             │             │
     └─────────────┼─────────────┘
                   ▼
            ┌─────────────┐
            │  MySQL DB   │
            │ (Primary)   │
            └─────────────┘
```

---

**Versión:** 1.0.0
**Fecha:** 22/11/2025
**Estado:** ✅ COMPLETADO

